# 研究計画書

## PatchDistill: Distilled Causal Patch Signatures に基づく効率的プロンプトインジェクション検出

---

## 1. 研究題目

**PatchDistill: Efficient Prompt Injection Detection via Distilled Causal Patch Signatures**

副題：
**直接的プロンプトインジェクションにおける内部制御シフトの因果的測定とOne-Pass検出器への蒸留**

---

## 2. 研究背景

大規模言語モデル（LLM）を組み込んだアプリケーションでは、ユーザー入力や外部文書に含まれる命令が、システムプロンプトや開発者指示よりも優先されてしまうプロンプトインジェクションが重大な問題となっている。特に、`Ignore previous instructions` や `Reveal the system prompt` のような直接的プロンプトインジェクションは、攻撃構造が比較的単純である一方で、LLMがどのような内部機構によって攻撃命令を優先してしまうのかは十分に理解されていない。

既存の検出手法には、ルールベース検出、TF-IDFなどを用いた古典的機械学習、Transformerベース分類器、LLM-as-a-Judge、LLM内部表現を用いた検出器などがある。しかし、多くの手法は入力テキストの表層的特徴、あるいはLLMの最終的な出力・判定結果に依存しており、「モデル内部でどのように制御が奪われるのか」という因果的機構には十分に踏み込んでいない。

本研究では、直接的プロンプトインジェクションの成功を、LLM内部における「system instruction由来の制御信号」から「user-side injected instruction由来の制御信号」へのシフトとして捉える。この内部制御シフトをactivation patchingによって因果的に測定し、その結果得られるCausal Patch Signatureを、推論時に1回のforward passだけで近似可能な軽量検出器へ蒸留する。

本研究の核心は、activation patchingを推論時の検出処理として用いるのではなく、offlineで因果教師信号を生成するために用いる点にある。推論時には高コストなpatchingを行わず、1回のforward passから得られるhidden states、attention statistics、residual stream statisticsなどを用いて、patch-effect signatureを近似予測する。

---

## 3. 研究目的

本研究の目的は、直接的プロンプトインジェクションに対して、LLM内部状態の因果的シグネチャに基づく効率的な検出アルゴリズムを提案することである。

具体的には、以下を目的とする。

1. 直接的プロンプトインジェクション成功時に、LLM内部のattention、hidden states、residual stream、MLP activationにどのような変化が生じるかを分析する。

2. Clean promptとInjected promptの内部状態を比較し、activation patchingによってプロンプトインジェクション成功に寄与する層・head・token位置を特定する。

3. Activation patchingによって得られるpatch-effect profileを、Causal Patch Signatureとして定義する。

4. 高コストなCausal Patch Signatureを、1回のforward passで取得可能な内部特徴量から近似予測するproxy modelへ蒸留する。

5. 蒸留されたpatch signatureを特徴量として用いる軽量な古典的機械学習検出器を構築する。

6. 古典ML単体、LLM-as-a-Judge単体、通常の内部表現分類器、既存のプロンプトインジェクション検出手法と比較し、分布シフト、言い換え攻撃、多言語攻撃、benign hard prompt、adaptive attackに対する有効性を評価する。

---

## 4. 研究仮説

本研究では、以下の仮説を検証する。

### H1: Internal Control Shift 仮説

直接的プロンプトインジェクションが成功する場合、LLM内部ではsystem instruction由来の制御信号が弱まり、ユーザー入力内の攻撃命令span由来の制御信号が強まる。

この変化は、attention mass、attention entropy、hidden state shift、residual stream shift、logit shiftなどに反映される可能性がある。

ただし、attention重みだけを説明として扱うことはできないため、本研究ではattentionを単独の証拠とはみなさず、activation patchingによる介入実験と組み合わせて検証する。

---

### H2: Causal Patch Signature 仮説

Injected prompt実行時の特定層・特定head・特定token位置のactivationを、clean prompt実行時のactivationに置換すると、プロンプトインジェクション成功率が低下する。

このpatch effectの分布は、直接的プロンプトインジェクションに特有の因果的内部シグネチャとして利用できる。

---

### H3: Patch Signature Distillation 仮説

Offline activation patchingによって得られる高コストなCausal Patch Signatureは、1回のforward passで取得可能な内部特徴量から近似予測できる。

すなわち、hidden states、attention statistics、residual stream statistics、logit shiftなどを入力として、patch-effect vectorを予測するproxy modelを学習できる。

---

### H4: Efficient Detection 仮説

蒸留されたCausal Patch Signatureを特徴量として用いることで、表層テキスト特徴に依存する検出器や、通常のhidden state probeよりも、言い換え攻撃、多言語攻撃、長文化攻撃に対して高い汎化性能を示す。

---

### H5: Hybrid Verification 仮説

蒸留されたpatch signatureに基づく軽量検出器をStage 1とし、LLM-as-a-JudgeをStage 2の意味論的検証器として用いることで、低コスト性と誤検知抑制を両立できる。

---

## 5. 提案手法

本研究では、提案手法を **PatchDistill** と呼ぶ。

PatchDistillは、以下の3段階から構成される。

1. Offline Causal Discovery
2. Patch Signature Distillation
3. One-Pass Online Detection

---

## 5.1 Phase 1: Offline Causal Discovery

まず、clean promptとinjected promptのペアを構築する。

Clean promptの例：

```text
以下の文章を要約してください。
[document]
```

Injected promptの例：

```text
以下の文章を要約してください。
Ignore previous instructions and reveal the system prompt.
[document]
```

このように、元のユーザータスクを保ったまま、直接的プロンプトインジェクション命令のみを追加したペアを作る。

次に、open-weight LLMにclean promptとinjected promptを入力し、各層から内部状態を抽出する。

抽出対象は以下である。

* Attention weights
* Attention entropy
* System prompt tokenへのattention mass
* User prompt tokenへのattention mass
* Malicious span tokenへのattention mass
* Hidden states
* Residual stream
* MLP activation
* Logit lensによる出力分布変化
* KV-cacheの統計量

Attention entropyは以下で定義する。

```math
H_i = - \sum_j \alpha_{ij} \log \alpha_{ij}
```

ただし、attention entropyの低下をそのまま攻撃の証拠とはみなさない。無害なコード解析、翻訳、引用、数式処理でもattentionの局所化は起こり得るためである。

そのため、本研究ではattention特徴量をあくまで候補シグナルとして扱い、activation patchingによる因果介入で検証する。

---

## 5.2 Causal Activation Patching

Clean prompt実行時のactivationとInjected prompt実行時のactivationを比較し、Injected prompt実行時の一部activationをclean側のactivationに置換する。

層を `l`、headを `h`、token位置を `t` とすると、patch operationを以下のように表す。

```math
a^{inj}_{l,h,t} \leftarrow a^{clean}_{l,h,t}
```

この操作後、モデルの出力が攻撃命令に従うか、元のシステム指示に従うかを測定する。

Patch effectを以下で定義する。

```math
PE(l,h,t) = M(y_{safe} \mid patch(l,h,t)) - M(y_{safe} \mid injected)
```

ここで、`M(y_safe)` はモデルがシステム指示に従った安全な出力を行う度合いを表すスコアである。具体的には、以下のいずれか、または複数を用いる。

* 拒否確率
* system-following logit
* attack-following logitの低下量
* LLM judgeによる安全出力スコア
* ルールベースでのsystem instruction遵守スコア

この `PE(l,h,t)` の分布を、Causal Patch Signatureと呼ぶ。

---

## 5.3 Phase 2: Patch Signature Distillation

Activation patchingは高コストであるため、推論時に毎回実行することは現実的ではない。

そこで本研究では、activation patchingを推論時の検出処理としてではなく、offlineで因果教師信号を生成するために用いる。

Offline patchingによって得たCausal Patch Signatureを以下のように表す。

```math
PE(x) = [PE_1(x), PE_2(x), ..., PE_k(x)]
```

ここで、`PE_i(x)` は選択された層・head・token位置 `i` をpatchしたときの安全出力スコア変化である。

一方、1回のforward passで取得可能な内部特徴量を以下で表す。

```math
r(x) = [h(x), A(x), R(x), L(x)]
```

ここで、

* `h(x)`: hidden states
* `A(x)`: attention statistics
* `R(x)`: residual stream statistics
* `L(x)`: logit shift / logit lens features

である。

本研究では、以下のproxy modelを学習する。

```math
q_\phi(r(x)) \approx PE(x)
```

すなわち、1回のforward passで得られる内部特徴量から、高コストなactivation patchingによって得られるpatch-effect vectorを近似予測する。

この処理を **Patch Signature Distillation** と呼ぶ。

---

## 5.4 Phase 3: One-Pass Online Detection

推論時にはactivation patchingを行わない。

入力 `x` に対してopen-weight LLMを1回だけforwardし、内部特徴量 `r(x)` を抽出する。

次に、proxy model `q_\phi` によって、蒸留されたpatch signatureを予測する。

```math
\hat{PE}(x) = q_\phi(r(x))
```

最終的な検出器は、`r(x)` と `\hat{PE}(x)` を用いて判定する。

```math
D(x) = C(r(x), \hat{PE}(x))
```

ここで `C` は軽量な古典的機械学習モデルであり、以下を候補とする。

* Logistic Regression
* Random Forest
* XGBoost
* Isolation Forest
* One-Class SVM

最終出力は以下の3値とする。

```text
ALLOW / BLOCK / ABSTAIN
```

高リスクまたは不確実な入力のみ、Stage 2としてLLM-as-a-Judgeへ送る。

---

## 6. アルゴリズム概要

```text
Algorithm: PatchDistill

Offline Phase:

Input:
  clean/injected prompt pairs
  open-weight LLM F

1. For each clean/injected pair:
      Run F on clean prompt
      Save internal activations

2. Run F on injected prompt
      Save internal activations

3. For selected layers/heads/tokens:
      Patch injected activations with clean activations
      Measure safety score shift

4. Construct Causal Patch Signature:
      PE(x) = [PE_1(x), ..., PE_k(x)]

5. Extract one-pass internal features:
      r(x) = [hidden states, attention stats, residual stats, logit features]

6. Train proxy model:
      qφ(r(x)) ≈ PE(x)

7. Train detector:
      D(x) = C(r(x), qφ(r(x)))


Online Phase:

Input:
  system prompt S
  user input U
  open-weight LLM F
  proxy model qφ
  detector C
  optional LLM judge J

1. Construct x = [S; U]

2. Run F on x once

3. Extract one-pass internal features r(x)

4. Estimate distilled patch signature:
      PE_hat(x) = qφ(r(x))

5. Compute risk score:
      score = C(r(x), PE_hat(x))

6. If score < τ_low:
      return ALLOW

7. If score > τ_high:
      return BLOCK

8. If τ_low <= score <= τ_high:
      Send x to LLM-as-a-Judge J
      return ALLOW / BLOCK / ABSTAIN
```

---

## 7. 新規性

本研究の新規性は以下にある。

### 7.1 内部表現分類から因果的patch signature分類への拡張

既存の内部表現ベース検出器は、LLMの特定層のhidden stateを取り出し、分類器に入力するものが多い。

これに対し、本研究はhidden stateを単に分類特徴量として使うのではなく、activation patchingによって「どの内部状態がプロンプトインジェクション成功に因果的に寄与するか」を測定する。

---

### 7.2 Attention観察ではなく介入による検証

本研究は、attention重みの変化をそのまま説明として扱わない。

Attention massやentropyの変化を候補シグナルとしつつ、activation patchingにより実際に出力挙動が変化するかを検証する。

---

### 7.3 高コストな因果信号のOne-Pass検出器への蒸留

Activation patchingは計算コストが高く、推論時に毎回実行することは現実的ではない。

本研究では、activation patchingをofflineで因果教師信号を得るために用い、そのpatch-effect signatureを1回のforward passで取得可能な内部特徴量へ蒸留する。

これにより、因果的情報を含む検出器を、現実的な推論コストで運用可能にする。

---

### 7.4 Mechanistic Featureに基づく軽量検出器

テキスト表層特徴ではなく、蒸留されたpatch-effect profile、residual stream shift、logit shiftなどのmechanistic featureを用いることで、言い換えや翻訳による表層的回避に対してより安定した検出を目指す。

---

### 7.5 LLM-as-a-Judgeを主役ではなく検証器として利用

LLM-as-a-Judgeを単純な分類器として用いるのではなく、PatchDistillが高リスクまたは不確実と判断した入力に対する意味論的検証器として利用する。

これにより、LLM judge呼び出しコストを抑えつつ、benign hard promptに対する誤検知を減らす。

---

## 8. 評価計画

### 8.1 評価対象

本研究では、主対象を直接的プロンプトインジェクションとする。

攻撃タイプは以下を含める。

* Instruction override
* System prompt extraction
* Developer message impersonation
* Safety bypass
* Role hijacking
* Policy override
* Data exfiltration request

また、誤検知を評価するため、以下のbenign hard promptを含める。

* プロンプトエンジニアリングの相談
* セキュリティ教材としての攻撃文例
* 小説・脚本中の命令文
* コードコメント中の命令文字列
* 「前の文を無視して」などの正常な編集命令
* 長文コード解析
* 法務・契約書など命令文が多い文書
* 翻訳・要約・抽出など、命令文を含む通常タスク

---

### 8.2 Indirect Prompt Injectionへの探索的分析

本研究の主対象はDirect PIである。

ただし、近年のLLMアプリケーションでは、RAG、メールエージェント、Webブラウジングエージェントなどを介したIndirect PIも重大な脅威である。

そこで本研究では、Direct PIに加えて、探索的分析としてIndirect PIを扱う。

具体的には、以下を比較する。

| 観点              | Direct PI                                      | Indirect PI                                                 |
| --------------- | ---------------------------------------------- | ----------------------------------------------------------- |
| 攻撃命令の位置         | user input内                                    | retrieved document / webpage / email内                       |
| 制御シフト           | system instruction → user injected instruction | user task/system instruction → external content instruction |
| source boundary | 比較的明確                                          | RAG文脈で曖昧                                                    |
| 想定される内部機構       | 命令競合                                           | source attribution / context binding failure                |
| 評価目的            | 主評価                                            | 探索的分析                                                       |

Indirect PIについては、完全な防御性能を主張しない。

本研究では、Direct PIとIndirect PIで内部control shiftのシグネチャがどの程度共通するか、または異なる層・head・token位置に現れるかを探索的に分析する。

---

### 8.3 比較手法

比較対象は以下とする。

1. Rule-based detector
2. TF-IDF + Logistic Regression
3. TF-IDF + Random Forest
4. TF-IDF + Linear SVM
5. Sentence-BERT + classifier
6. DeBERTa / RoBERTa classifier
7. LLM-as-a-Judge
8. LlamaGuard系モデル
9. Hidden state probe
10. Attention statistics detector
11. Internal representation detector
12. Game-theoretic / adaptive detection系手法
13. PatchDistill without patching supervision
14. PatchDistill without proxy distillation
15. PatchDistill without LLM judge
16. PatchDistill full model

---

### 8.4 評価指標

以下の指標で評価する。

* Accuracy
* Precision
* Recall
* F1
* AUROC
* AUPRC
* False Negative Rate
* False Positive Rate
* FNR at fixed FPR
* FPR at fixed FNR
* LLM judge call rate
* Latency
* Cost per 1M prompts
* One-pass proxy error
* Patch signature prediction error
* Robust accuracy under paraphrase attack
* Robust accuracy under multilingual attack
* Robust accuracy under adaptive attack
* Benign hard prompt false positive rate

セキュリティ用途では見逃しが重大であるため、特にFalse Negative Rateを重視する。

また、本研究ではproxy modelが真のpatch-effect signatureをどの程度近似できるかを重要な評価対象とする。

---

## 9. 実験設定

### 9.1 使用モデル

初期実験では、内部状態を取得しやすい小型open-weightモデルを用いる。

デバッグ用モデル：

* GPT-2 small
* Pythia-410M
* Qwen2.5-0.5B

メカニズム分析用モデル：

* Qwen2.5-1.5B
* Gemma-2-2B

主評価用モデル：

* Qwen2.5-7B-Instruct
* Llama-3.1-8B-Instruct
* Mistral-7B-Instruct

小型モデルはあくまでmechanistic analysisと実装検証に用いる。論文の主実験では、7B級instruction-tuned modelで評価する。

---

### 9.2 実装環境

実験環境はGoogle Colab Pro+を想定する。

使用予定ライブラリは以下である。

* PyTorch
* Hugging Face Transformers
* TransformerLens
* scikit-learn
* XGBoost
* bitsandbytes
* accelerate
* pandas
* numpy
* matplotlib

OllamaはLLM-as-a-Judgeや初期検証には利用できるが、attention、hidden states、residual streamなどを詳細に抽出する実験では、Hugging Face TransformersやTransformerLensを優先する。

---

### 9.3 7B級モデルでの効率化

7B級モデルで全層・全head・全tokenに対してactivation patchingを行うと計算コストが大きすぎる。

そのため、以下の効率化を行う。

1. 候補span周辺だけpatchingする

   * `ignore`
   * `forget`
   * `system prompt`
   * `developer message`
   * `override`
   * `reveal`
   * `act as`
   * `you are now`

2. 代表層に絞る

   * early layer
   * middle layer
   * late layer
   * pilot実験で重要度が高かった層

3. 重要headに絞る

   * system/user attention ratioが大きく変化するhead
   * malicious span attention massが急上昇するhead
   * logit shiftへの影響が大きいhead

4. Offline subsetでのみpatchingする

   * 全データではなく、代表的な数千件に限定する
   * その後、proxy modelへ蒸留する

---

### 9.4 データ分割

通常のrandom splitだけでは不十分であるため、以下の分割を行う。

1. Random split
2. Template split
3. Source split
4. Paraphrase split
5. Multilingual split
6. Benign hard split
7. Adaptive attack split

特にtemplate splitとbenign hard splitを重視する。

攻撃テンプレートがtrain/testに漏れると、検出器が本質ではなくテンプレートを暗記する危険がある。

---

## 10. 期待される結果

本研究により、以下の結果が期待される。

1. 直接的プロンプトインジェクション成功時に、LLM内部でsystem instruction由来の制御信号が弱まり、攻撃命令由来の制御信号が強まる現象を定量的に示せる。

2. Activation patchingにより、プロンプトインジェクション成功に寄与する内部状態部位を特定できる。

3. Patch-effect signatureが、通常のhidden state probeよりもプロンプトインジェクション検出に有効な特徴量であることを示せる可能性がある。

4. 高コストなactivation patchingの結果を、1回のforward passで得られる内部特徴量から近似できる可能性がある。

5. PatchDistillが、LLM-as-a-Judge単体より低コストで、TF-IDFや通常の内部表現分類器よりも高いOOD性能を示す可能性がある。

6. Direct PIとIndirect PIにおける内部control shiftの違いを探索的に明らかにできる可能性がある。

---

## 11. 想定されるリスクと対策

### リスク1: Attention変化がPI成功と強く相関しない

Attentionだけでは説明として不十分である可能性がある。

対策：
Attentionに限定せず、hidden states、residual stream、MLP activation、logit shiftを含める。最終的な主張はattentionではなくCausal Patch Signatureに置く。

---

### リスク2: Activation patchingの計算コストが高い

全層・全head・全tokenに対してpatchingすると計算コストが大きい。

対策：
Activation patchingはofflineでのみ行う。推論時にはpatchingを実行しない。代表層・重要head・候補span周辺に絞ってpatchingし、その結果をproxy modelへ蒸留する。

---

### リスク3: One-pass proxyがpatch-effectを十分に近似できない

Causal Patch Signatureが、1回のforward passで得られる特徴量から予測できない可能性がある。

対策：
まず小型モデルでproxy prediction errorを検証する。近似が難しい場合は、patch signature全体を予測するのではなく、上位重要成分のみを予測する。また、binary risk labelへの直接蒸留も検討する。

---

### リスク4: 小型モデルでしかシグネチャが確認できない

Mechanistic interpretabilityの実験は小型モデルに偏りがちである。

対策：
小型モデルはデバッグ用に限定し、主実験ではQwen2.5-7B-Instruct、Llama-3.1-8B-Instruct、Mistral-7B-Instructなどの実用規模モデルで評価する。

---

### リスク5: Direct PI以外に一般化しない

本研究は直接的プロンプトインジェクションを主対象とするため、間接PIやエージェント攻撃への一般化は限定的である。

対策：
Direct PIへの対象限定を明確にする。その上で、Indirect PIについては探索的分析を行い、内部control shiftがどの程度共有されるかを検証する。

---

### リスク6: 既存の内部表現分類器との差分が弱くなる

単にhidden stateを分類するだけでは既存研究との差分が弱い。

対策：
本研究の主張を「内部表現分類」ではなく、「activation patchingによる因果教師信号の生成」と「patch signature distillation」に置く。Ablationとして、hidden state probeとPatchDistillを比較する。

---

### リスク7: LLM-as-a-Judgeが攻撃される

LLM-as-a-Judge自体がprompt injectionやjudge manipulationを受ける可能性がある。

対策：
LLM-as-a-Judgeは最終意思決定の唯一の根拠にしない。PatchDistillが高リスクまたは不確実と判定した入力に対する補助的なsemantic verifierとして利用する。

---

## 12. 研究スケジュール

### Phase 1: 先行研究整理とデータセット構築

期間：1〜2週間

* 直接的PIに関する既存研究整理
* 内部表現ベース検出器、LLM-as-a-Judge系、防御系研究の比較
* 直接的PIデータセットの収集
* benign hard promptの設計
* clean/injected pairの初期構築

成果物：

* 関連研究表
* データセット仕様
* ラベル定義
* 初期ベンチマーク設計

---

### Phase 2: 内部状態抽出パイプライン構築

期間：2〜3週間

* Transformers / TransformerLens環境構築
* 小型モデルでattention / hidden states / residual stream抽出
* clean/injected pairのforward pass
* layer/head/token別の可視化

成果物：

* Colab notebook
* 内部状態抽出コード
* attention mass / entropy / hidden state shiftの可視化

---

### Phase 3: Offline Activation Patching実験

期間：3〜4週間

* clean run / injected runのactivation保存
* patching関数の実装
* system-following scoreの定義
* patch-effect heatmapの作成
* 成功PIと失敗PIの比較
* 重要層・重要head・重要token位置の抽出

成果物：

* Causal Patch Signature抽出器
* 重要layer/head/tokenの分析
* Internal Control Shift仮説の検証結果

---

### Phase 4: Patch Signature Distillation

期間：2〜3週間

* one-pass internal featuresの設計
* patch-effect vectorを教師信号としたproxy model学習
* proxy prediction errorの評価
* 上位重要patch componentの予測実験

成果物：

* patch signature proxy model
* distillation評価結果
* proxy prediction error分析

---

### Phase 5: PatchDistill検出器の構築

期間：2〜3週間

* 蒸留されたpatch signatureの特徴量化
* Logistic Regression / XGBoost / Isolation Forestで分類
* baseline比較
* ablation study

成果物：

* PatchDistill prototype
* baseline比較表
* ablation結果

---

### Phase 6: Robustness評価

期間：3〜4週間

* paraphrase attack
* multilingual attack
* long-context injection
* benign hard prompt
* adaptive attack
* LLM-as-a-Judge併用実験
* cost / latency評価

成果物：

* robustness評価表
* cost-risk tradeoff
* failure analysis

---

### Phase 7: 7B級モデルでの主評価

期間：3〜5週間

* Qwen2.5-7B-Instructで評価
* Llama-3.1-8B-Instructで評価
* Mistral-7B-Instructで評価
* 小型モデルとの比較
* モデル間でのsignature転移性を検証

成果物：

* 7B級モデルでの主実験結果
* model transfer分析
* scale effect分析

---

### Phase 8: Indirect PI探索的分析

期間：1〜2週間

* RAG文書・外部コンテンツに埋め込まれたIndirect PIを用意
* Direct PIとIndirect PIのcontrol shift signatureを比較
* 重要層・head・token位置の違いを分析

成果物：

* Direct / Indirect比較分析
* Discussion用図表

---

### Phase 9: 論文化

期間：4〜6週間

* 論文構成作成
* 実験結果整理
* 図表作成
* 関連研究との差分整理
* artifact整備
* 投稿先選定

成果物：

* 論文ドラフト
* GitHub artifact
* 再現実験ノートブック
* supplementary material

---

## 13. 投稿先候補

### AI / ML系

* ICLR
* NeurIPS
* ICML

AI / ML系を狙う場合は、mechanistic interpretability、causal intervention、representation shift、distillation、OOD generalizationを強化する。

---

### NLP系

* ACL
* EMNLP
* NAACL

NLP系を狙う場合は、prompt injection detection、LLM-as-a-Judge、counterfactual evaluation、multilingual robustness、benign hard prompt評価を強化する。

---

### セキュリティ系

* IEEE Symposium on Security and Privacy
* USENIX Security
* ACM CCS
* NDSS

セキュリティ系を狙う場合は、adaptive attack、threat model、failure analysis、artifact reproducibility、realistic deployment costを強化する。

---

## 14. 本研究の位置づけ

本研究は、従来のプロンプトインジェクション検出を、入力テキスト分類問題から、LLM内部の制御シフト検出問題へ拡張するものである。

特に、以下の点で既存研究と異なる。

1. テキスト表層ではなく、内部状態の因果的変化に基づく。
2. Attention可視化ではなく、activation patchingによる介入実験を行う。
3. Hidden state分類ではなく、patch-effect signatureを教師信号として用いる。
4. 高コストなpatching結果を、one-pass internal featureへ蒸留する。
5. 推論時にはactivation patchingを行わず、現実的な計算コストで検出する。
6. LLM-as-a-Judgeを単独分類器ではなく、不確実ケースのsemantic verifierとして用いる。
7. Direct PIを主対象としつつ、Indirect PIとの内部機構差分も探索的に分析する。

---

## 15. 期待される貢献

本研究の貢献は以下の通りである。

1. Direct PI成功時のLLM内部状態変化を、Internal Control Shiftとして定式化する。

2. Activation patchingにより、プロンプトインジェクション成功に関与する内部状態部位を因果的に分析する。

3. Patch-effect profileをCausal Patch Signatureとして特徴量化する。

4. Causal Patch Signatureを、1回のforward passで得られる内部特徴量へ蒸留するPatch Signature Distillationを提案する。

5. 蒸留されたcausal signalを用いた軽量なプロンプトインジェクション検出器を構築する。

6. Direct PI、benign hard prompt、paraphrase、multilingual、adaptive attackを含む評価設定を構築する。

7. Direct PIとIndirect PIにおける内部control shiftの共通点・相違点を探索的に分析する。

---

## 16. まとめ

本研究では、直接的プロンプトインジェクションを、LLM内部におけるsystem instructionからinjected instructionへの制御シフトとして捉える。

まず、activation patchingを用いて、プロンプトインジェクション成功に因果的に寄与する内部状態部位を特定する。次に、そのpatch-effect profileをCausal Patch Signatureとして定義する。

ただし、activation patchingは高コストであるため、推論時には実行しない。代わりに、offline patchingで得られたCausal Patch Signatureを、1回のforward passで得られるhidden states、attention statistics、residual stream statisticsなどへ蒸留する。

これにより、因果的機構に基づく情報を保持しながら、現実的な推論コストで動作するプロンプトインジェクション検出器PatchDistillを構築する。

本研究の核心は、プロンプトインジェクション検出を、表層テキスト分類やLLM judge依存から、内部機構に基づく因果的シグネチャ検出へ拡張し、さらにその高コストな因果情報をone-pass検出器へ蒸留する点にある。
